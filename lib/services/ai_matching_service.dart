class AiMatchingService {
  static final AiMatchingService instance = AiMatchingService();
  Future<List<Map<String, dynamic>>> findMatches({
    required dynamic targetItem,
    required List<dynamic> candidates,
  }) async {
    return [];
  }
}
